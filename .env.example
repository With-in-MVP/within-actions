# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Auth secrets (shared with Auth0 Actions)
INTERNAL_API_KEY=generate-a-strong-random-secret
WITHIN_ACTION_SECRET=generate-a-strong-random-secret

# Enforcement SDK auth (shared with vendor MCP backends)
WITHIN_API_KEY=generate-a-strong-random-secret

# Scoring (future — Claude-based ICP scoring)
ANTHROPIC_API_KEY=sk-ant-...

# Server
PORT=4300
